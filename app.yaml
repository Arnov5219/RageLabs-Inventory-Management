runtime: python312
instance_class: F1

entrypoint: gunicorn -b :$PORT --workers 2 --threads 4 laundryrage_project.wsgi:application

handlers:
- url: /static
  static_dir: staticfiles/

- url: /.*
  script: auto
